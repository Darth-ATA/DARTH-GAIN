# ──────────────────────────────────────────────
# SDD Config — DARTH-GAIN
# Auto-generated by sdd-init
# ──────────────────────────────────────────────

project: darth-gain
stack: python
description: "Automate your Hevy workout progression for hypertrophy gains"
repo: https://github.com/Darth-ATA/DARTH-GAIN
build: hatchling
python: ">=3.11"
entry_point: darth_gain.cli:cli

persistence:
  mode: hybrid
  engram: true
  openspec: true

strict_tdd: true
strict_tdd_reason: "pytest runner configured in pyproject.toml; no explicit TDD marker found — default enabled"

testing:
  runner:
    tool: pytest
    command: "pytest"
    config: "[tool.pytest.ini_options] testpaths = ['tests']"
  layers:
    unit: true
    integration: false
    e2e: false
  coverage:
    available: false
    command: null
  quality:
    linter:
      tool: ruff
      command: "ruff check"
      available: true
    type_checker:
      tool: null
      command: null
      available: false
    formatter:
      tool: ruff
      command: "ruff format"
      available: true

phases:
  proposal: {}
  spec: {}
  design: {}
  tasks:
    review_workload_forecast: true
  apply: {}
  verify:
    test_required: true
    test_command: "pytest"
  archive: {}
