# DARTH-GAIN Skill Registry

> Auto-generated by sdd-init on 2026-07-20.
> Scanned user skill dirs; no project-level skills found.

## Convention Files

None found in this project.

---

## Skills

### branch-pr

- **Trigger**: creating, opening, or preparing PRs for review
- **Path**: `~/.claude/skills/branch-pr/SKILL.md`
- **Rules**:
  - Every PR MUST link an approved (`status:approved`) issue — no exceptions.
  - Every PR MUST have exactly one `type:*` label.
  - Blank PRs without issue linkage are blocked by GitHub Actions.
  - Branch names MUST match `^(feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert)\/[a-z0-9._-]+$`.
  - PR body must include: Linked Issue, PR Type, Summary, Changes Table, Test Plan, Contributor Checklist.
  - All automated checks must pass before merge.
  - Conventional commits required: `type(scope): description`.
  - Run `shellcheck` on modified scripts before pushing.

### chained-pr

- **Trigger**: PRs over 400 lines, stacked PRs, review slices
- **Path**: `~/.claude/skills/chained-pr/SKILL.md`
- **Rules**:
  - Split PRs over **400 changed lines** unless a maintainer explicitly accepts `size:exception`.
  - Each PR must be reviewable in ≤60 minutes.
  - Keep tests/docs with the unit they verify.
  - Every child PR must include a dependency diagram marking the current PR with `📍`.
  - Use Stacked PRs (to main) when each slice can land independently.
  - Use Feature Branch Chain (with tracker) when the feature must integrate before main.
  - Do not mix chain strategies after the user chooses one.
  - Polluted diffs must be retargeted or rebased until only the current work unit appears.

### cognitive-doc-design

- **Trigger**: writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs
- **Path**: `~/.claude/skills/cognitive-doc-design/SKILL.md`
- **Rules**:
  - Lead with the answer — put the decision, action, or outcome first.
  - Progressive disclosure: happy path first, then details, edge cases, references.
  - Chunk related info into small sections; keep flat lists short.
  - Signpost with headings, labels, callouts, summaries.
  - Prefer tables, checklists, examples over prose.
  - State what to review first and what is intentionally out of scope in PR docs.
  - Default structure: outcome title → quick path → details table → checklist → next step.

### comment-writer

- **Trigger**: PR feedback, issue replies, reviews, Slack messages, or GitHub comments
- **Path**: `~/.claude/skills/comment-writer/SKILL.md`
- **Rules**:
  - Start with the actionable point — do not recap the whole PR before feedback.
  - Be warm and direct, like a thoughtful teammate, not a corporate bot.
  - Keep to 1–3 short paragraphs or a tight bullet list.
  - Explain the technical reason when asking for a change.
  - Avoid pile-ons — comment on the highest-value issue, not every tiny preference.
  - Match the target context language in comments (Spanish thread → Spanish comment, English thread → English).
  - No em dashes — use commas, periods, or parentheses instead.
  - Formula: Direct observation → Why it matters (if needed) → Concrete next action.

### find-skills

- **Trigger**: "how do I do X", "find a skill for X", "is there a skill that can..."
- **Path**: `~/.claude/skills/find-skills/SKILL.md`
- **Rules**:
  - Use `npx skills find [query]` to search for skills by keyword.
  - Use `npx skills add <owner/repo@skill>` to install a skill.
  - Use `-g -y` flags for global silent install.
  - Browse available skills at https://skills.sh/.
  - If no skill found, offer to help directly or suggest creating one with `npx skills init`.

### go-testing

- **Trigger**: Go tests, go test coverage, Bubbletea teatest, golden files
- **Path**: `~/.claude/skills/go-testing/SKILL.md`
- **Rules**:
  - Prefer table-driven tests with `t.Run(tt.name, ...)` for multiple cases.
  - Test behavior and state transitions, not implementation trivia.
  - Use `t.TempDir()` for filesystem tests.
  - Keep integration tests skippable with `testing.Short()`.
  - For Bubbletea: test `Model.Update()` directly for state; use `teatest` only for interactive flows.
  - Golden files must be deterministic; update only through the repo's `-update` path.
  - Use small mocks/interfaces around system boundaries.

### issue-creation

- **Trigger**: creating GitHub issues, bug reports, or feature requests
- **Path**: `~/.claude/skills/issue-creation/SKILL.md`
- **Rules**:
  - Blank issues are disabled — MUST use a template (bug report or feature request).
  - Every issue gets `status:needs-review` automatically on creation.
  - A maintainer MUST add `status:approved` before any PR can be opened.
  - Questions go to Discussions, not issues.
  - Search existing issues for duplicates before creating a new one.
  - Fill in ALL required fields and check pre-flight checkboxes.
  - Bug Report template: pre-flight checks, bug description, steps to reproduce, expected/actual behavior, OS, agent/client, shell.
  - Feature Request template: pre-flight checks, problem description, proposed solution, affected area.

### judgment-day

- **Trigger**: "judgment day", "dual review", "adversarial review", "juzgar"
- **Path**: `~/.claude/skills/judgment-day/SKILL.md`
- **Rules**:
  - Replaces ordinary 4R review for the target — never run both simultaneously.
  - Build one complete immutable target, then launch two blind read-only judges in parallel with identical scope.
  - Wait for both judges; never accept a partial judgment.
  - Never launch `review-refuter` — two-judge agreement is the corroboration mechanism.
  - Fix only severe findings confirmed by both judges. WARNING/SUGGESTION rows remain `info`.
  - Permit at most two fix rounds and two scoped re-judgments.
  - Terminal states: `approved` or `escalated` — never reset or extend an exhausted lineage.
  - If judges contradict, escalate for explicit human decision.

### skill-creator

- **Trigger**: new skills, agent instructions, documenting AI usage patterns
- **Path**: `~/.claude/skills/skill-creator/SKILL.md`
- **Rules**:
  - Create a skill only when a pattern is reused and AI needs guidance, or project conventions differ from generic practice.
  - Do not create skills for trivial, one-off, or documentation-only cases.
  - Skill body target: 180–450 tokens; hard max 1000 tokens.
  - Use required structure: Activation Contract, Hard Rules, Decision Gates, Execution Steps, Output Contract, References.
  - Frontmatter MUST include `name`, `description` (one line, ≤250 chars, quoted, trigger-first), `license`, `metadata.author`, `metadata.version`.
  - References must point to local files; supporting material goes in `assets/` or `references/`.
  - No `Keywords` section — preserve trigger words in `description`.

### skill-improver

- **Trigger**: improve skills, audit skills, refactor skills, skill quality
- **Path**: `~/.claude/skills/skill-improver/SKILL.md`
- **Rules**:
  - Use `skill-creator` instead for brand-new skills; this is for auditing/improving existing ones.
  - Treat `docs/skill-style-guide.md` as the normative style contract (fallback: `references/skill-style-guide.md`).
  - Default to audit-only — modify files only when the user explicitly asks to apply improvements.
  - Never delete meaningful content silently; move long material to `references/` or `assets/`.
  - Do not invent triggers, policies, or domain rules — mark ambiguous cases for human review.
  - Use `.atl/skill-registry.md` as the index for skill paths.

### work-unit-commits

- **Trigger**: implementation, commit splitting, chained PRs, keeping tests and docs with code
- **Path**: `~/.claude/skills/work-unit-commits/SKILL.md`
- **Rules**:
  - Commit by work unit (deliverable behavior/fix/migration/docs), NOT by file type.
  - Keep tests with the code they verify (same commit).
  - Keep docs with the user-visible change they explain.
  - Each commit should tell a story — a reviewer should understand why it exists from its diff and message.
  - Each commit should be a candidate chained PR when the change grows.
  - If SDD tasks forecast >400-line change, group into chained PR slices before implementation.
  - Before committing: confirm one clear purpose, repo makes sense after this commit alone, rollback is reasonable, tests/docs included, focused test command recorded.
  - Rollback boundary must name exact files/behavior removable without unrelated work.
